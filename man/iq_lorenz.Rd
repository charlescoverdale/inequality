% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lorenz.R
\name{iq_lorenz}
\alias{iq_lorenz}
\title{Lorenz curve}
\usage{
iq_lorenz(x, weights = NULL, na.rm = FALSE)
}
\arguments{
\item{x}{Numeric vector of incomes (non-negative).}

\item{weights}{Optional numeric vector of survey weights.}

\item{na.rm}{Logical. Remove \code{NA} values? Default \code{FALSE}.}
}
\value{
An S3 object of class \code{"iq_lorenz"} with elements:
\describe{
\item{curve}{data.frame with columns \code{cum_pop} and \code{cum_income}
(both 0 to 1). Starts at (0, 0) and ends at (1, 1).}
\item{gini}{Numeric. The Gini coefficient (twice the area between
the curve and the diagonal).}
\item{n}{Integer. Number of observations.}
}
}
\description{
Computes the Lorenz curve: the cumulative share of income held by the
cumulative share of the population, ordered from poorest to richest.
The result can be plotted with \code{plot()}.
}
\examples{
d <- iq_sample_data("income")
lc <- iq_lorenz(d$income)
plot(lc)
}
